import { Options } from 'prettier';

const settings = {
    semi: true,
    trailingComma: 'all',
    singleQuote: true,
    printWidth: 120,
    tabWidth: 4,
    endOfLine: 'auto',
} as Options;

export default settings;
